```java
{
	'genre': 	如果为None，则不用考虑；如果有，为电影的类型。
	'country':	如果为None，则不用考虑；如果有，则为国家。
	'personName':	如果为None，则不用考虑；如果有，则为人的名字(包括导演、演员、编剧等)。
	'movieName':	如果为None，则不用考虑；如果有，则为电影名字。
	'time':			默认值为(0,0)，则不用考虑；如果不为(0,0),则第一个数字为时长的最小值，第二个数字为时长的最大值。
	'year':			默认值为(0,0)，则不用考虑；如果不为(0,0),则第一个数字为起始年份，第二个数字为终止年份。
	'score':{
		'role':		默认值为None,则不用考虑；如果为'score'，则表示以分数为基准；如果为'rank'，则表示以排名为基准。
		'value':	默认值为None，则不用考虑；如果有数字，看'role'，如果为分数，则表示基准分数，如果为排名，则表示基本排名。
		'count':	默认值为None，则不用考虑；如果有数字，则表示要查询的数据条数。
		'flag':		默认值为None，则不用考虑；当'role'为'score'时，该值有效。为1表示大于'value'，为2表示小于'value'，为3表示等于'value'。
	}
}
查询建议：movieName建议用包含，因为数据库中的name包括中英文。
```

